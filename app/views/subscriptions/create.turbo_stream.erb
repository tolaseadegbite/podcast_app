<%= turbo_stream.replace 'subscription_button' do %>
    <%= render 'subscriptions/subscription_button', subscribable: @subscription.subscribable %>
<% end %>
<%= render_turbo_stream_flash_messages %>