<ul class="nav justify-content-between container fw-bold channel-nav text-uppercase">
    <li class="nav-item">
        <%= link_to 'Channels', profile_path(@user.username), class: "nav-link text-secondary #{ 'text-dark' if controller_name == 'profiles' && action_name == 'show' }" %>
    </li>

    <li class="nav-item">
        <%= link_to 'Subscriptions', user_subs_path(@user.username), class: "nav-link text-secondary #{ 'text-dark' if controller_name == 'profiles' && action_name == 'user_subscriptions'}" %>
    </li>

    <li class="nav-item">
        <%= link_to 'Likes', user_likes_path(@user.username), class: "nav-link text-secondary  #{ 'text-dark' if controller_name == 'profiles' && action_name == 'user_likes'}" %>
    </li>

    </li>
    <li class="nav-item">
        <%= link_to 'About me', about_user_path(@user.username), class: "nav-link text-secondary #{ 'text-dark' if controller_name == 'profiles' && action_name == 'about_user'}" %>
    </li>
</ul>