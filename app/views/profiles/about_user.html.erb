<% provide(:title, "#{@user.username}'s subscriptions".html_safe) %>

<!-- layout for channel page and subpages i.e channels home, episodes, subscriptions etc.. -->
<%= render 'profiles/shared/profile_layout' %>

<!-- Profile edit modal -->
<%= render 'profiles/edit_profile_modal' %>